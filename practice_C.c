#include <stdio.h>
#include <wiringPi.h>





int main(){

   //pinMode(LED_board, OUTPUT);

   while(1){

        /*
        digitalWrite(LED_board, HIGH);
        delay(500);
        digitalWrite(LED_board, LOW);
        delay(500);
        */
       printf("hello");
        delay(1000);
   }
                      

    return 0;
}